import numpy as np


naive = np.array([0.653310, 1.283886, 2.489766, 4.739629, 6.764706, 10.027726])
print(f"naive / single     : {naive / naive[0]}")
naive_opt = np.array([5.163149, 8.566996, 15.021120, 16.625426, 20.246109, 22.890871])
print(f"naive_opt / single : {naive_opt / naive_opt[0]}")
print(f"naive_opt / naive  : {naive_opt / naive[0]}")
omp = np.array([7.3434040, 14.425432, 25.796978, 47.830557, 66.460208, 73.962038])
print(f"omp / single       : {omp / omp[0]}")
print(f"omp / naive        : {omp / naive[0]}")
mpi = np.array([5.593204, 10.866776, 20.389943, 37.212980, 48.469855, 49.933255])
print(f"mpi / single       : {mpi / mpi[0]}")
print(f"mpi / naive        : {mpi / naive[0]}")

naive = np.array([16.644326, 17.141220, 17.733793])
naive_opt = np.array([20.917516, 27.443318, 27.159379])
print(f"naive_opt / naive         : {naive_opt / naive}")

omp_blocked = np.array([33.246826, 39.626707, 37.686406])
print(f"omp_blocked / naive        : {omp_blocked / naive}")
print(f"omp_blocked / naive_opt    : {omp_blocked / naive_opt}")
omp_oblivious = np.array([75.037455, 29.762205, 34.177494])
print(f"omp_oblivious / naive      : {omp_oblivious / naive}")
print(f"omp_oblivious / naive_opt  : {omp_oblivious / naive_opt}")
omp_timeskew = np.array([89.080177, 44.555941, 51.096243])
print(f"omp_timeskew  / naive      : {omp_timeskew / naive}")
print(f"omp_timeskew  / naive_opt  : {omp_timeskew / naive_opt}")
omp_circqueue = np.array([14.925003, 13.950418, 14.555035])
print(f"omp_circqueue  / naive     : {omp_circqueue / naive}")
print(f"omp_circqueue  / naive_opt : {omp_circqueue / naive_opt}")
omp_simd = np.array([29.934117, 30.294345, 36.650655])
print(f"omp_simd  / naive          : {omp_simd / naive}")
print(f"omp_simd  / naive_opt      : {omp_simd / naive_opt}")
omp_intrinsic = np.array([30.487998, 31.756734, 35.174399])
print(f"omp_intrinsic  / naive     : {omp_intrinsic / naive}")
print(f"omp_intrinsic  / naive_opt : {omp_intrinsic / naive_opt}")
omp = np.array([86.475845, 69.101709, 67.793709])
print(f"omp / naive                : {omp / naive}")
print(f"omp / naive_opt            : {omp / naive_opt}")

mpi_naive = np.array([43.487911, 37.450291, 37.573451])
print(f"mpi_naive / naive          : {mpi_naive / naive}")
print(f"mpi_naive / naive_opt      : {mpi_naive / naive_opt}")
mpi_inaive = np.array([44.423804, 37.622439, 35.789594])
print(f"mpi_inaive / naive         : {mpi_inaive / naive}")
print(f"mpi_inaive / naive_opt     : {mpi_inaive / naive_opt}")
mpi_blocked = np.array([57.834689, 49.288444, 39.606897])
print(f"mpi_blocked / naive        : {mpi_blocked / naive}")
print(f"mpi_blocked / naive_opt    : {mpi_blocked / naive_opt}")
mpi = np.array([54.476819, 50.423912, 40.542475])
print(f"mpi / naive                : {mpi / naive}")
print(f"mpi / naive_opt            : {mpi / naive_opt}")
